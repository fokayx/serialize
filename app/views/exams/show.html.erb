<h1>套餐名稱：<%= @exam.name %></h1>
<p>檢查項目如下，按照項目分類列表 </p>
<% @exam.category.each do |category| %>
  <div>
    <p> 項目分類：<%= category %></p>
    <ul>
      <% @exam.items.where(category: category).each do |item| %>
        <li>項目：<%= item.name %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= link_to '修改套餐基本資料', edit_exam_path(@exam) %>
<%= link_to '新增套餐檢查項目', add_item_exams_path(@exam) %>
